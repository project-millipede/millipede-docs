import { selectors as interaction } from './interaction/reducer';
import { selectors as post } from './post/reducer';
import { selectors as timeline } from './timeline/selector';

export const selectors = {
  interaction,
  post,
  timeline
};
