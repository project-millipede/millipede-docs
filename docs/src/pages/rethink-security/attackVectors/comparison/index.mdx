import WrappedByExample from './use';

# Comparison of established and recent attack vectors

The following table compares the similarity of the problems with regard to the
objective, effect, cause and tools used by the attacker / actor. The reader
interprets the table from the bottom up.

<WrappedByExample />
